# Bytecode Obfuscation

This document is intentionally deferred for v1.

## Status

Out of scope for the first-pass bytecode feature.

## Notes

If bytecode serialization becomes stable enough to ship, obfuscation or
encryption can be revisited later as a separate concern. It should not block
the minimal compile/load path.
